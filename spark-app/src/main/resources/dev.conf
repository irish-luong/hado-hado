mode = "dev"
kafka {
    bootstrap {
        servers = "broker:29092"
    }
    topics {
        demoTopic = "hado.topic.network"
    }
}

spark {
    master = "spark://spark-master:7077"
    topics {
        demoTopic = "hado.topic.network"
    }
}